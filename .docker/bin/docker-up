#!/usr/bin/env bash

project_name="SANDBOX"

./check-env.sh

echo "Starting docker services for ${project_name} project..."
docker-compose \
    -p ${project_name} \
    -f ../docker-compose.yml \
    up -d "$@"
